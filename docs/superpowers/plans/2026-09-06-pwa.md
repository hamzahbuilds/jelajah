# v0.22 PWA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** Ship spec v0.22 (installable PWA + read-only offline), e2e green.
**Spec:** docs/10-spec-v0.22-pwa.md (approved, binding — the writes-never-intercepted guardrail is the review's automatic-fail check).

## Global Constraints
Standard battery (RM0/no deps; no server route changes; money untouched; t() EN+BM; no commits; vitest 163+/tsc/build per task; full ritual final task with v0.22 marker; kill pattern ps-aux; wipe .wrangler/state per attempt).

### Task 1: Manifest, icons, service worker
**Files:** `public/manifest.webmanifest`, `public/icon.svg`, `public/icons/*.png` (generated once via a dev-only scripts/make-icons.mjs Playwright screenshot of the SVG — script committed, PNGs committed), `public/sw.js`, `index.html` (manifest + theme-color + apple-touch-icon links), `vite.config.ts` ONLY IF public/ isn't already copied (verify against tess/ precedent first).
- [ ] sw.js per spec §2 exactly: method check FIRST (only GET ever handled), class routing, versioned caches with manual V constant + header doc, activate cleanup, no skipWaiting.
- [ ] Manifest + icons render (maskable padding on the SVG variant).
- [ ] Verify: build; confirm dist/ contains sw.js + manifest + icons; vitest/tsc.

### Task 2: Client registration + offline banner + update banner
**Files:** `src/lib/pwa.ts` (+ pure helper `classifyResponse(headers)` if useful, tested), `src/main.tsx` (register, PROD-only), `src/api.ts` (detect X-Jelajah-Offline on responses → notify listener; do NOT change error semantics), `src/App.tsx` (banner component in Chrome: offline variant + update-available variant with Refresh button), `src/components/` (banner), `src/i18n.tsx` (offlineBanner, updateBanner, refresh — en+ms), logout hook → `clearApiCaches()` (find logout in App.tsx).
- [ ] TDD any pure helper; banner dark-safe, non-blocking, dismiss not needed (auto-clears).
- [ ] api.ts change is additive (response header sniff) — every existing test/flow unchanged.
- [ ] Verify trio.

### Task 3: e2e + full ritual + closeout
**Files:** `scripts/e2e.mjs`, `docs/build-status.md`.
- [ ] Steps per spec §4 in a FRESH browser context at the END of the suite (so SW caching can't pollute earlier steps); unregister/clean context after. If offline-render proves flaky across 2 attempts, downgrade per spec's recorded ruling (registration + cache-population assertions) and note it.
- [ ] Marker → v0.22; FULL ritual; build-status v0.22 section + deferred (offline writes v2, push, licensed photos).

## Self-review notes
- SW + e2e interaction is the risk; spec pre-authorizes the downgrade ruling.
- public/ copy verified against tess/ precedent before touching vite config.
- api.ts touch is the only shared-file risk — additive header sniff only.
