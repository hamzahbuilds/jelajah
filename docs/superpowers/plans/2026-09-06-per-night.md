# v0.26 Per-Night Room Splits Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** Ship spec v0.26 nights-as-weights splitting, e2e green at v0.26. MONEY-CRITICAL: conservation + v0.25 regression-suite-unchanged are automatic-fail checks.
**Spec:** docs/14-spec-v0.26-per-night.md (binding).

## Global Constraints
Standard battery; every v0.25 roomSplit test passes UNCHANGED (proves default-path freeze); split_json old-shape tolerance in drift; ritual final task; ps-aux kill; wipe .wrangler/state.

### Task 1: Engine weights + schema
**Files:** shared/roomSplit.ts (occupantWeights per spec §2, additive; 'invalid_weight' code), tests/roomSplit.test.ts (NEW cases appended — existing cases byte-untouched), server/lib/schema.ts (room_occupants.nights, SCHEMA+UPGRADES), migrations/0001_init.sql (regenerate + scratch-sqlite check).
- [ ] TDD new cases; verify: vitest/tsc/build/db:local; diff proves old tests untouched.

### Task 2: Server — occupants nights + weighted resolve
**Files:** server/app.ts (occupants PUT nights validation ladder per spec §3 incl. stayNights bound; resolveRoomsSplit weights; split_json snapshot {id,nights}; GET rooms occupants array).
- [ ] Read v0.25 routes first; existing paths frozen; wrangler smoke: set nights via curl, create weighted split, hand-computed assert, invalid_nights 400s.

### Task 3: Client — nights UI + drift both-shape
**Files:** src/components/RoomsCard.tsx (nights badge + stepper, editor-gated), src/components/ExpenseForm.tsx (person-nights occupancy label + weighted prefill — existing modes frozen), src/lib/roomsDrift.ts + tests (both-shape snapshots, nights-sensitive), src/i18n.tsx, src/styles.css.
- [ ] Verify trio; e2e breakage list for T4.

### Task 4: e2e + ritual + closeout
**Files:** scripts/e2e.mjs (fixture stay gets dates if missing; weighted steps w/ hand-computed asserts; drift-on-nights; marker v0.26), docs/build-status.md.
- [ ] FULL ritual green (2 preferred); build-status v0.26.

## Self-review notes
- Engine contract shifts default-resolution to the caller — spec §2 documents why (mixed defaults need stay context); T2 review must verify the server's weight derivation (nights ?? stayNights ?? 1).
- Old-shape split_json tolerance is the compat trap — T3 tests both shapes.
