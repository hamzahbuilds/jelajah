# v0.24 Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** Ship spec v0.24 (dashboard/currency layout, sidebar foot + language placement, plan chip alignment, plan action-visibility redesign), e2e green at v0.24.
**Spec:** docs/12-spec-v0.24-polish.md (binding).

## Global Constraints
Standard battery; behavior-freeze list = spec §4 handler freeze (automatic-fail); e2e Move-up selectors preserved via Edit-mode-on step; kill pattern ps-aux; wipe .wrangler/state.

### Task 1: Dashboard + Currency layout (spec §1)
**Files:** src/pages/Dashboard.tsx (wrap Currency+UpNext in 2-col grid ≥1024px), src/components/FxWidget.tsx (sparkline svg → width 100%/preserveAspectRatio none/h72; header row restructure — LAYOUT ONLY, zero data/handler changes), src/styles.css.
- [ ] Rhythm audit: consistent 16px gaps in the dashboard stack (find missing margins).
- [ ] Verify trio; screenshot before/after at 1280px + 390px (scratchpad, view them).

### Task 2: Sidebar + language (spec §2)
**Files:** src/components/Sidebar.tsx (remove select + bare logout; add Log out nav-item; align side-user row to .ic grid), src/App.tsx (mobile topbar: remove lang select), src/pages/Settings.tsx (verify Language seg PATCHes /me lang — wire if not), src/components/navModel.ts only if foot model changes, src/styles.css (side-user alignment, collapsed-rail logout), tests/nav.test.ts if model touched.
- [ ] Log out item calls session logout (read how Sidebar gets it — useSession).
- [ ] Collapsed rail: icon-only logout; alignment check both states (screenshot).
- [ ] e2e: grep for `.side-lang` / topbar select selectors (T8-era language steps!) — language e2e steps must move to Settings seg; update intent-preserved.
- [ ] Verify trio.

### Task 3: Plan chip alignment + action redesign (spec §3+§4)
**Files:** src/pages/Plan.tsx, src/styles.css, src/i18n.tsx, scripts/e2e.mjs (Edit-mode-on step before Move-up uses; optional mobile ⋯ assert).
- [ ] Chip: left-column alignment per spec §3 (solid bg, line continuation) — screenshot verify.
- [ ] Edit-mode toggle (desktop) + per-row ⋯ Menu (mobile) per spec §4; handlers byte-identical (freeze); checkbox always visible; drag handle desktop-edit-mode-only.
- [ ] Menu reuses shared Menu component; Delete in menu = danger + existing confirm flow.
- [ ] Verify trio + `node --check scripts/e2e.mjs`.

### Task 4: Full ritual + closeout
**Files:** scripts/e2e.mjs, docs/build-status.md.
- [ ] Marker v0.24; FULL ritual to green (2 clean preferred); build-status v0.24 section.

## Self-review notes
- e2e language steps (sidebar .side-lang) are known breakage — T2 owns the move-to-Settings rewrite.
- Edit-mode default OFF changes default DOM → T3 must add the enable step to EVERY e2e block using row actions (grep thoroughly: Move up/down, edit pencil, delete, drag).
- FxWidget layout-only rule mirrors the money freeze discipline.
