# v0.21 Rooms Allocation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** Ship spec v0.21 rooms allocation end-to-end, e2e green.
**Spec:** docs/09-spec-v0.21-rooms.md (approved, binding).

## Global Constraints
Same battery as v0.20 (RM0/no deps; SCHEMA+UPGRADES both + regenerate migrations/0001_init.sql via the same node extraction used in the v0.20 ticket batch; money/split untouched; t() EN+BM; Icon+viewBox; dark overrides; 44px; integer/exists guards on new routes per v0.20 precedent; audit+trackUsage on writes; no commits; vitest+tsc+build per task; full ritual final task, kill pattern ps-aux based).

---

### Task 1: Server — schema, rooms API, cascade, MCP context, pure helper

**Files:** `server/lib/schema.ts` (two tables SCHEMA+UPGRADES CREATE TABLE IF NOT EXISTS — idempotent as-is), `migrations/0001_init.sql` (regenerate), `server/app.ts` (5 routes per spec §API + trip-delete cascade + MCP/AI context rooms section), `shared/rooms.ts` + `tests/rooms.test.ts`.
**Produces:** routes exactly as spec; `shared/rooms.ts`: `occupancyLabel(count, infants, capacity): string` ("3/4", "3+1/4", "3" when no capacity) and `singleOccupancyBatch(roomId, participantIds, siblingRoomIds): {deleteFrom: number[]}` pure helpers, tested (TDD).
- [ ] TDD shared/rooms.ts; schema both places; regenerate migrations; routes with requireEditor (grep how plan edits gate and reuse), participant validation mirroring activities; trip-delete cascade grows to include room_occupants then rooms (order matters, FK); MCP context: find where day_notes enter the AI/MCP trip context and append rooms compactly.
- [ ] Verify: vitest (new tests green), tsc, build, `npm run db:local`, scratch-sqlite check of regenerated migrations (same python one-liner as v0.20 batch).

### Task 2: Client — People page open to all + Rooms card + nav

**Files:** `src/pages/People.tsx` (restructure: rooms card for ALL, existing member/invite/details/danger cards stay canLead-gated), `src/components/navModel.ts` + `tests/nav.test.ts` (People item now for all roles — update the leader-only test cases deliberately, note in report), `src/components/TabBar.tsx` More-sheet gating if it mirrors navModel (check), `src/i18n.tsx`.
**Consumes:** T1 routes.
- [ ] Rooms card per spec §UI: stay-group sections, suggested-stay one-tap picker + free-text add, room chips with occupancyLabel badge (warning class when over), per-room "+ add" person-picker pattern (lighter choice, record), tap-occupant-remove for editors, infant badge + capacity exclusion, viewer read-only.
- [ ] navModel: People for all; nav tests updated; e2e member-visibility steps will change in T3 (the old "People hidden for editor" assertions — flag them for T3, don't fix here unless build breaks).
- [ ] Verify trio; i18n parity.

### Task 3: e2e + full ritual + closeout

**Files:** `scripts/e2e.mjs`, `docs/build-status.md`.
- [ ] Update any existing assertions that People is leader-only (now: visible to all, but invites/details/danger cards still leader-only — assert BOTH sides: editor sees rooms card, editor does NOT see danger card).
- [ ] New steps per spec §e2e (stay+rooms+assign+over-capacity badge+move person single-occupancy+viewer read-only+trip-delete cascade still green).
- [ ] Extend PASSED marker to v0.21; FULL ritual to green; build-status v0.21 section + deferred list (PWA, licensed photos, room-cost splitting v2).

## Self-review notes
- CREATE TABLE IF NOT EXISTS is inherently idempotent — UPGRADES entries are the same statements, safe.
- People-for-all is the one behavioral widening; server side People-page data: /participants is leader-gated? T2 must ensure the rooms card fetches only /trips/:id/rooms (member-ok) and doesn't call leader-only endpoints for non-leaders.
- Trip-delete cascade order: room_occupants before rooms before participants-linked tables.
